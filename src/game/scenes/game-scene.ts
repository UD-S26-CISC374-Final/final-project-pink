/**  2. Interactive Logic:
   * Card Interaction: Clicking a card "highlights" it and adds its ID to the CaseManager.
   * Slot Constraints: If selectedEvidence.length >= evidenceSlots, prevent further selection (provide a "Slots Full" visual cue).
   * "Rest My Case" Button: A prominent button that only appears/becomes active once the required slots are filled. Clicking this triggers the
     transition to VerdictScene.
 */